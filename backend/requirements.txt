# ASTRAMIND Backend - Production Requirements (Render-safe)
# ── Core Framework ───────────────────────────────────────────
fastapi>=0.111
uvicorn[standard]>=0.30
gunicorn>=21.2.0
python-dotenv>=1.0.0

# ── Data Validation & Settings ───────────────────────────────
pydantic>=2.7
pydantic[email]>=2.7
pydantic-settings>=2.2

# ── Authentication & Security ────────────────────────────────
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
pyjwt>=2.8.0
bcrypt>=4.1.3
cryptography>=43.0.0
email-validator>=2.2.0

# ── Database & ORM ───────────────────────────────────────────
sqlalchemy>=2.0
asyncpg>=0.29
aiosqlite>=0.20.0
psycopg[binary]>=3.1
sqlalchemy-utils>=0.41.1
alembic>=1.12.1

# ── HTTP & Networking ────────────────────────────────────────
httpx>=0.27.0
aiohttp>=3.9
requests>=2.31.0

# ── Web Parsing & Content ────────────────────────────────────
beautifulsoup4>=4.12.3
lxml>=5.2.1
python-multipart>=0.0.9
bleach>=6.1.0
markdown>=3.6
duckduckgo-search>=6.0.0

# ── Payment ──────────────────────────────────────────────────
razorpay>=1.4.2

# ── Utilities ────────────────────────────────────────────────
psutil>=5.9.0
filetype>=1.2.0
cachetools>=5.3.3
anyio>=4.0.0
orjson>=3.10.0
python-decouple>=3.8.0
click>=8.1.7

# ── Rate Limiting ────────────────────────────────────────────
slowapi>=0.1.8

# ── Monitoring & Logging ─────────────────────────────────────
prometheus-client>=0.20.0
sentry-sdk>=2.0.0
loguru>=0.7.2

# ── Optional Tracing (graceful fallback if unavailable) ──────
opentelemetry-sdk>=1.26.0
opentelemetry-exporter-otlp>=1.26.0
opentelemetry-instrumentation-fastapi>=0.47b0
